# Нехай у рядку міститься послідовність слів,
# що розділена одним чи декількома символами пропуску.
# Необхідно видалити зайві пропуски між словами, так,
# щоб слова розділялися лише одним символом пропуску.
# Також видалити всі пропуски на початку та вкінці рядка.

s = "    hello    hello     word    " # => "hello hello word"
# s = input("Задайте рядок ")

l_words = s.split()

s_cleaned = " ".join(l_words)

print(s_cleaned)


